# .NET
bin/
obj/
*.user
*.suo
*.cache
.vs/
*.dll
*.exe
*.pdb
TestResults/
*.csproj.user
.dotnet/
packages/

# Build output of `dotnet pack` — generated per build, never commit.
# Tracked .nupkg files here get picked up by the publish workflow's verify/push steps.
nupkgs/
*.nupkg

# Blazor
**/wwwroot/css/app.css*

# Tailwind
node_modules/
tailwindcss.exe

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
NUL

# Environment
.env
.env.local
.env.*.local

# Auto-extracted NuGet docs
.trblazeui/

# Development Tools
.playwright-mcp
.devflow
.claude
.tfcore
.opencode/
DEV-TASKS.md
.instructions
.reviews
/CLAUDE.md
/WORKFLOW.html

# TechieFlow framework — deployed copies, never commit (managed by scaffold/update-framework.sh)
/opencode.jsonc
/.tf-scaffold-note.txt

# TechieFlow agent artifacts — machine-generated test harness & logs, never commit (managed by scaffold/update-framework.sh)
/package.json
/package-lock.json
test-results/
playwright-report/
.verify/
logs/
/docs/.last-verify.json

# TechieFlow agent artifacts — machine-generated test harness & logs, never commit (managed by scaffold/update-framework.sh)
tests/.artifacts/
test-results-*/

# TechieFlow agent artifacts — machine-generated test harness & logs, never commit (managed by scaffold/update-framework.sh)
/scripts-*/
