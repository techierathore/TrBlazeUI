@namespace TrBlazeUI.Components.Avatar

@if (isLoaded && !hasError)
{
    <img src="@Source"
         alt="@Alt"
         class="@CssClass"
         @attributes="AdditionalAttributes"
         @onerror="HandleError" />
}
