@namespace TrBlazeUI.Components.Separator

<div
    role="@(Decorative ? "none" : "separator")"
    aria-orientation="@(Decorative ? null : Orientation.ToString().ToLower())"
    class="@CssClass"
    @attributes="AdditionalAttributes">
</div>
