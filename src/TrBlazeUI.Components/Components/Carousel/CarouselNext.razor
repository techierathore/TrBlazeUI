@namespace TrBlazeUI.Components.Carousel
@using TrBlazeUI.Icons.Lucide.Components

<button type="button"
        class="@CssClass"
        disabled="@(!Context?.CanGoNext)"
        @onclick="HandleClick"
        aria-label="Next slide" @attributes="AdditionalAttributes">
    <LucideIcon Name="arrow-right" Class="h-4 w-4" />
    <span class="sr-only">Next slide</span>
</button>

@code {
    [CascadingParameter]
    public Carousel? Context { get; set; }

    [Parameter]
    public string? Class { get; set; }

    private void HandleClick()
    {
        Context?.Next();
    }

    private string PositionClass => Context?.Orientation == CarouselOrientation.Horizontal
        ? "-right-12 top-1/2 -translate-y-1/2"
        : "-bottom-12 left-1/2 -translate-x-1/2 rotate-90";

    private string CssClass => TrBlazeUI.Components.Utilities.ClassNames.cn(
        "absolute h-8 w-8 rounded-full",
        "inline-flex items-center justify-center",
        "border bg-background shadow-sm",
        "hover:bg-accent hover:text-accent-foreground",
        "disabled:pointer-events-none disabled:opacity-50",
        PositionClass,
        Class
    );

    /// <summary>
    /// Gets or sets additional HTML attributes (id, style, data-*, aria-*, event handlers)
    /// forwarded to the rendered root element.
    /// </summary>
    [Parameter(CaptureUnmatchedValues = true)]
    public Dictionary<string, object>? AdditionalAttributes { get; set; }
}
