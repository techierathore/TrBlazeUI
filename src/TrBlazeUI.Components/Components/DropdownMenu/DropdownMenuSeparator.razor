@namespace TrBlazeUI.Components.DropdownMenu

<div class="@CssClass" role="separator" aria-orientation="horizontal"></div>

@code {
    /// <summary>
    /// Gets or sets additional CSS classes to apply to the separator.
    /// </summary>
    [Parameter]
    public string? Class { get; set; }

    /// <summary>
    /// Gets the computed CSS classes for the separator.
    /// </summary>
    private string CssClass
    {
        get
        {
            var baseClasses = "-mx-1 my-1 h-px bg-muted";
            return string.IsNullOrWhiteSpace(Class) ? baseClasses : $"{baseClasses} {Class}";
        }
    }
}
