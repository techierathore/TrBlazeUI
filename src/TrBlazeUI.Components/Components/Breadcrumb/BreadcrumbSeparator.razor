@namespace TrBlazeUI.Components.Breadcrumb
@using TrBlazeUI.Icons.Lucide.Components

<li role="presentation" aria-hidden="true" class="@CssClass">
    @if (ChildContent != null)
    {
        @ChildContent
    }
    else
    {
        <LucideIcon Name="chevron-right" Class="h-3.5 w-3.5" />
    }
</li>

@code {
    [Parameter]
    public string? Class { get; set; }

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    private string CssClass => TrBlazeUI.Components.Utilities.ClassNames.cn(
        "[&>svg]:w-3.5 [&>svg]:h-3.5",
        Class
    );
}
