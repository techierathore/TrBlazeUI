namespace TrBlazeUI.Components.Sidebar;

/// <summary>
/// Size variants for SidebarMenuButton.
/// </summary>
public enum SidebarMenuButtonSize
{
    /// <summary>
    /// Small size with reduced padding and text (text-xs).
    /// </summary>
    Small,

    /// <summary>
    /// Default size with standard padding and text.
    /// </summary>
    Default,

    /// <summary>
    /// Large size with increased padding and text (text-base).
    /// </summary>
    Large
}
