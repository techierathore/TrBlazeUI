@namespace TrBlazeUI.Components.ContextMenu

<div class="@CssClass" style="@TriggerStyle" @oncontextmenu="HandleContextMenu" @oncontextmenu:preventDefault>
    @ChildContent
</div>

@code {
    [CascadingParameter]
    public ContextMenu? Context { get; set; }

    [Parameter]
    public string? Class { get; set; }

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    private async Task HandleContextMenu(MouseEventArgs e)
    {
        if (Context != null)
        {
            await Context.OpenAtAsync(e.ClientX, e.ClientY);
        }
    }

    private string CssClass => TrBlazeUI.Components.Utilities.ClassNames.cn(
        "w-fit",
        Class
    );

    private string? TriggerStyle => Context?.IsOpen == true
        ? "position: relative; z-index: 41;"
        : null;
}
