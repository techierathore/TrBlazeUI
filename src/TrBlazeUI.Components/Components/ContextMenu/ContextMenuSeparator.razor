@namespace TrBlazeUI.Components.ContextMenu

<div role="separator" class="@CssClass"></div>

@code {
    [Parameter]
    public string? Class { get; set; }

    private string CssClass => TrBlazeUI.Components.Utilities.ClassNames.cn(
        "-mx-1 my-1 h-px bg-muted",
        Class
    );
}
