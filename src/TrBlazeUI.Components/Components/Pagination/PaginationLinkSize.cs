namespace TrBlazeUI.Components.Pagination;

/// <summary>
/// Specifies the size variant of a pagination link.
/// </summary>
public enum PaginationLinkSize
{
    /// <summary>The default size, sized to fit its text label.</summary>
    Default,
    /// <summary>A square size suited to icon-only links.</summary>
    Icon
}
